# Figures

Drop screenshots here with these exact filenames and the placeholders in the
poster are replaced automatically (no LaTeX edits needed):

| filename | what to capture |
|---|---|
| `mapping_page.png` | Pangenome Mapping result card: identity-ranked assembly list, filter box, "Position on haplotype" section with browser/new tab/details links |
| `pangenome_seq_track.png` | hgTracks showing the "Pangenome Seq" track with base-level mismatch coloring |
| `convert_page.png` | Convert Coordinates page: scored destination picker + hgConvert-style result (`chrX:... (Y% of bases, Z% of span)`) |
| `lifted_annotations.png` | An HPRC sample assembly with the lifted "Annotations from CHM13" track group (CAT/Liftoff, RefSeq, CenSat, T2T Encode) + the Alignment Differences track |
| `ucsc_logo.png` | UCSC Genomics Institute logo (header, left) |
| `hprc_logo.png` | HPRC logo (header, right) |

PNG at 150+ dpi at the printed size works well (roughly 1500+ px wide for the
column-width shots).
